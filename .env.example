# Globe.gl Configuration (no env vars needed - uses public CDN)
# Mapbox is no longer used; globe.gl handles Earth visualization

# Optional: Uncomment below if adding Mapbox features to other pages in the future
# NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk_your_mapbox_access_token_here
# NEXT_PUBLIC_MAPBOX_STYLE=mapbox://styles/mapbox/dark-v11
